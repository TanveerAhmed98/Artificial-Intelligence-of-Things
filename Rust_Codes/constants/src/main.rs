/*constants are the values which are bound to a name and we don't want to change
  these values later in the program, we also need to declare data type while creating 
  constants.they can be declared outside the main function*/
  const BOX_WEIGHT:f32=66.8;
  fn main(){
        println!("Weight of box: {}",BOX_WEIGHT);
  }
